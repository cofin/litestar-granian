name: Tests And Linting

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: test-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install 3.12

      - name: Create virtual environment
        run: uv sync --locked

      - name: Install Pre-Commit hooks
        run: uv run pre-commit install

      - name: Load cached Pre-Commit Dependencies
        id: cached-pre-commit-dependencies
        uses: actions/cache@v6
        with:
          path: ~/.cache/pre-commit/
          key: pre-commit|${{ env.pythonLocation }}|${{ hashFiles('.pre-commit-config.yaml') }}

      - name: Execute Pre-Commit
        run: uv run pre-commit run --show-diff-on-failure --color=always --all-files

  mypy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync --locked

      - name: Run mypy
        run: uv run mypy

  pyright:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync --locked

      - name: Run pyright
        run: uv run pyright

  slotscheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync --locked

      - name: Run slotscheck
        run: uv run slotscheck litestar_granian

  test-linux:
    runs-on: ubuntu-latest
    env:
      UV_PYTHON: ${{ matrix.python-version }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14", "3.14t"]
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --locked

      - name: Verify base install has no optional loop
        run: uv run python tools/check_base_install.py

      - name: Test
        run: uv run pytest tests

  test-windows:
    runs-on: windows-latest
    env:
      UV_PYTHON: ${{ matrix.python-version }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13", "3.14", "3.14t"]
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --locked

      - name: Verify base install has no optional loop
        run: uv run python tools/check_base_install.py

      - name: Test
        run: uv run pytest tests
  test-osx:
    runs-on: macos-latest
    env:
      UV_PYTHON: ${{ matrix.python-version }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14", "3.14t"]
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --locked

      - name: Verify base install has no optional loop
        run: uv run python tools/check_base_install.py

      - name: Test
        run: uv run pytest tests

  test-event-loops-unix:
    runs-on: ${{ matrix.os }}
    env:
      UV_PYTHON: ${{ matrix.python-version }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - {os: ubuntu-latest, python-version: "3.14t", loop: asyncio, sync-args: ""}
          - {os: ubuntu-latest, python-version: "3.14t", loop: rloop, sync-args: "--extra rloop"}
          - {os: ubuntu-latest, python-version: "3.14", loop: uvloop, sync-args: "--extra uvloop"}
          - {os: ubuntu-latest, python-version: "3.14t", loop: uvloop, sync-args: "--extra uvloop"}
          - {os: macos-latest, python-version: "3.14t", loop: asyncio, sync-args: ""}
          - {os: macos-latest, python-version: "3.14t", loop: rloop, sync-args: "--extra rloop"}
          - {os: macos-latest, python-version: "3.14", loop: uvloop, sync-args: "--extra uvloop"}
          - {os: macos-latest, python-version: "3.14t", loop: uvloop, sync-args: "--extra uvloop"}
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install ${{ matrix.python-version }}

      - name: Install isolated loop dependencies
        run: uv sync --locked ${{ matrix.sync-args }}

      - name: Test explicit event loop
        run: uv run pytest tests/integration/test_event_loops.py
        env:
          LITESTAR_GRANIAN_TEST_LOOP: ${{ matrix.loop }}

  test-event-loops-windows:
    runs-on: windows-latest
    env:
      UV_PYTHON: ${{ matrix.python-version }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - {python-version: "3.14", loop: winloop, sync-args: "--extra winloop"}
          - {python-version: "3.14t", loop: winloop, sync-args: "--extra winloop"}
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install ${{ matrix.python-version }}

      - name: Install isolated loop dependencies
        run: uv sync --locked ${{ matrix.sync-args }}

      - name: Test explicit event loop
        run: uv run pytest tests/integration/test_event_loops.py
        env:
          LITESTAR_GRANIAN_TEST_LOOP: ${{ matrix.loop }}

  build-docs:
    needs:
      - validate
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync --locked

      - name: Build docs
        run: uv run make docs
